#include <stdio.h>
#include <stdlib.h>


int main()
{
    int num=0,ok,n,max=0,sumav,colb,randb,cola,suma,rand,i,j,A[50][50],B[50][50];
    char s1[200];
    char s2[200];
    char c[2];

    printf("Introduceti timp:\n");
   scanf("%d%d %d%d %d%d %s", i,j,a,b,x,y,c);
   
   
   for(i=i;i<3;i++){
    if(i==2){i=0;
    break;}
    
    for(j=j;j<10;j++)
    if(j==10)j=0;
   for(a=a;a<7;a++)if(a==6)a=0;
   for(b=b;b<11;b++)if(a==10)b=0;
   for(x=x;x<7;x++)if(x==6)x=0;
   for(y=y;y<11;y++)if(y==10){y=0;break;}



    return 0;
}
